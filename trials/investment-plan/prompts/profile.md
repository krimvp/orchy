Read `PROFILE.md` in the working directory.

Turn it into a plan shape, and no further.

- `goals` names what the money is for, in the words of the person.
- `constraints` names what the plan may not do. Read these off the file. Do
  not add one the file does not support.
- `sleeves` divides the portfolio. Give at least three. `target_pct` holds
  whole percents that add to 100. `name` names the asset class plainly, such
  as `global equity index` or `short-term bonds`, because a later step reads
  these names.

Recommend no specific fund here. Write no file.
