The segments and the incumbents are below. Give the reading.

- `verdict` is `wedge` when one segment is served badly by everything that
  holds the market, `crowded` when it is served well enough by several,
  `no market` when nobody pays for this at all, and `unclear` when the
  documents do not settle it.
- `crowding` says how full the market is.
- `note` is two sentences: the reason for the verdict, and the one fact that
  would overturn it.

Do not soften the verdict. Write no file.
