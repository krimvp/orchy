Read `TALK.md` in the working directory. It says what the speaker knows and
what the event is.

Outline the talk "{{ title }}" for {{ audience }}, in {{ minutes }} minutes.

- `thesis` is one sentence the audience should be able to repeat afterwards.
  Not a topic. A claim.
- `sections` each carry `minutes`, and they must add to the length of the talk
  with a few minutes spare. `point` says what that section proves, not what it
  covers.
- `cut_first` names the section to drop when the talk is running long.

If an earlier round listed things to fix, fix those first and keep the rest.

Write no file.
