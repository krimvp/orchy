Three critics read the outline through three lenses. Their problems are below.

Decide whether this outline is ready to write slides from.

- `must_fix` holds only the problems that would hurt the talk on the day.
  Merge the ones that are the same problem seen twice. Drop the ones that are
  taste.
- `approved` is false when `must_fix` holds anything, and true when it is
  empty.

A talk is never perfect. Approve one that works. The outline goes back to be
rewritten if you refuse it, and it can only go back twice.

Write no file.
