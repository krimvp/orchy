A market scan ran inside this flow, and a module sized the prize. Both are
below.

Read `IDEA.md` for what the founder is actually proposing.

- `call` is `build it`, `test it first`, or `drop it`. Weigh the scan and the
  arithmetic together: a wedge into a market too small to live on is not a
  business, and a large market held firmly by an incumbent is not an opening.
- `first_test` is the cheapest thing that would settle the question, doable in
  two weeks by one person, and it names what result would count.
- `kill_criterion` is what the founder should agree, now, would make them stop.

Write no file.
