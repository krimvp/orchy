A module scored the clauses, and at least one came back over three. The scores
are below.

Write the counter-offer.

- `asks` covers the clauses scored over three, worst first. Each `ask` is a
  sentence the tenant can paste into an email. Each `why` is the reason, in
  the tenant's interest, in one line.
- `walk_away_line` names the single term that, unchanged, should end the
  conversation.

Ask for less than everything. A counter that objects to the whole lease gets
no reply. Write no file.
