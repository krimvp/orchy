Read `LEASE.md` in the working directory.

Pull out every clause that puts an obligation, a cost, or a restriction on the
tenant. Give at least four.

- `name` is short and plain, such as `deposit` or `early termination`. A later
  step runs once for each name, so make them distinct.
- `text` quotes the clause as the lease writes it. Do not paraphrase.

`term_months` and `rent_monthly` come off the face of the lease.

Judge nothing here. Write no file.
