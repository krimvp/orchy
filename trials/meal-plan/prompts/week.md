Read `KITCHEN.md` in the working directory. It says who eats, what they will
not eat, and what the week looks like.

Decide which nights are being cooked for, and how long there is on each.

- `nights` holds one entry per night that needs a dinner. `name` is the day,
  and a later step runs once for each, so use each day once. `minutes` is what
  the household actually has that evening. `note` says what makes that night
  different.
- Leave out the nights the file says are already handled.
- `avoid` gathers every allergy, dislike, and refusal in the file.
- `staples_on_hand` is what they already have, so nobody buys it again.

Choose no dishes here. Write no file.
