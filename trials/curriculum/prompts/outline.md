Read `LEARNER.md` in the working directory. It says who is studying and why.

Outline a course in {{ subject }} for that person, in {{ weeks }} weeks.

- `modules` holds between four and six. `weeks` on each one adds to the total.
  `name` is short and distinct, because a later step runs once for each.
- `outcome` says what the learner can do at the end of the module, as a thing
  they do, not a thing they know about.
- `prerequisites` names what they must already have. Read the file. Do not
  list what they already told you they have.

Write no file.
