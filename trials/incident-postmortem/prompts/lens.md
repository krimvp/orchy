You read this incident through one lens, and it is below. Stay in it.

The timeline is in the values below. Read `INCIDENT.md` and `logs/` yourself
before you answer.

- `findings` holds at least two things this lens shows. A finding names a
  condition of the system, not a mistake by a person.
- `question_for_the_team` is the one question you would ask in the review that
  the documents cannot answer.

Blame nobody. Change no file.
