Read `INCIDENT.md` in the working directory, and the log excerpt in `logs/`.

Build the timeline. One entry for each thing that happened, in order, with the
time as the file gives it. Include the deploy, the first symptom, the first
alert, the first human, and the fix.

`impact` says what a user lost, in one sentence. Not what the system did.

Name no person. Change no file.
