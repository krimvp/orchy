A module walked the pot through four return paths. The results are below, and
they are the numbers: do not recompute them and do not adjust them.

Read `SITUATION.md` and `ASSUMPTIONS.md` in the working directory.

- `plain_english` says what the four paths mean for this household, in four
  sentences, without a percentage sign.
- `sequence_risk` explains why the order of the returns matters more than
  their average, using the paths above as the example.
- `guardrails` gives at least two rules the household could agree now: a
  spending rule, a rebalancing rule, a rule about when to take less.

Change no file.
