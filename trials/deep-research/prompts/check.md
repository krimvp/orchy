Read `BRIEF.md` in the working directory.

Take the claims that carry the most weight, up to six of them, and open the
URL each one cites. Check that the page says what the brief says it says.

`unsupported` names every claim whose source does not carry it, whose URL does
not open, or which the brief states more strongly than its source does.

`approved` is true only when `unsupported` is empty.

Change no file.
