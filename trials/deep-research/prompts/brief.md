Four readers each took one angle. Their claims are below.

Write the brief to `BRIEF.md` in the working directory. Change no other file.

- Open with the answer, in three sentences, for someone who reads nothing else.
- Then one section for each angle, holding the claims that survived.
- Every claim keeps its URL. A claim with no source does not appear.
- `disagreements` names where two readers say different things. Say which one
  the brief followed and why, in the file as well as in the value.
- Close with what nobody found, which is the part a reader most needs.

`claims` is how many claims the file carries.

If an earlier round listed unsupported claims, drop them or find a source.
