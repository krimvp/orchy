The question this run takes is below.

Turn it into something four readers can divide between them.

- `question` restates it in one sentence, sharp enough to be answered.
- `angles` holds exactly four. Each one is a different line of enquiry, not
  the same question in four voices. Number them in the order a reader should
  take them.
- `out_of_scope` names what a reader should not spend time on.

Use the web for nothing here. Write no file.
