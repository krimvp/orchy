Read `TRIP.md` in the working directory. It holds what the travellers want.

Decide the shape of the trip to {{ destination }}, and nothing else.

- `themes` names at least three threads the trip could follow.
- `pace` is `slow`, `steady`, or `packed`. Judge it by who is travelling.
- `must_do` holds what the travellers named themselves. Invent nothing here.

Write no file.
