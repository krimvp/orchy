Audit one dependency of this repository.

The value below names the package. Read `package.json` for the version this
repository asks for, then read the code that imports the package.

Answer with the name, the version, whether the package is risky to keep, and
the notes that support your answer. A package is risky when it is unmaintained,
when it is far behind, or when this repository uses an API that it deprecated.

Change nothing. This step promises that.
