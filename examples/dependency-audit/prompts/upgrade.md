The audit found a risky dependency. Read the findings and the advice below.

Edit `package.json` to ask for a version that is safe to take. Change no other
file: this step promises to change `package.json` and nothing else.

Answer with the entries you changed, and the reason.
