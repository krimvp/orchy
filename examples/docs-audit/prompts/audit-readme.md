Audit `README.md` against the code in `src/`.

Check every command, every file path, every dependency, and every claim about
what the program guarantees.

Read the rules in `prompts/audit.md` and follow them.
