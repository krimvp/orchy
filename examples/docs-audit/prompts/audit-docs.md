Audit every file under `docs/` against the code in `src/`.

Check the plan and each decision record. A decision record that states a rule
must match the code that enforces the rule.

Read the rules in `prompts/audit.md` and follow them.
