# Write the code

Make the change that the repository asks for.

If a value from a step before this one holds findings, correct every finding
first. Do not start new work while a finding is open.

Read the code around your change before you change it. Keep the change small.

When the work is complete, call `submit_result` once. Give a short summary and
the list of the files that you changed.
