Read `README.md` and write `{{ file }}`: the same document in {{ language }}.
Keep the code blocks, the file paths, and the names of tools as they are, and
translate everything else. Keep the tone plain, as the original keeps it.

Change nothing outside `docs/i18n`.
