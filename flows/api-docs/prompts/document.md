Read `{{ file }}` and write `docs/api/{{ name }}.md`: what the module is for,
what it exports, and one example of a call. Write for a reader who has not
opened the file. Change nothing outside `docs/api`.
