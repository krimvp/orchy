The step before you wrote tests. Read them and run them. Change no file.

A good test fails when the code breaks. Reject a test that asserts nothing, a
test that repeats the implementation line for line, and a test that passes
for the wrong reason.

Answer `approved: true` only when every new test earns its place. Put each
objection in `findings`, one sentence each.
