The step before you claims to have fixed an issue. Read its summary and the
files it names.

Check the claim: read the changed code, and run the test suite when the
project has one. Change no file.

Answer `passes: true` only when the fix does what the issue asks and breaks
nothing you can see. Put everything wrong in `findings`.
