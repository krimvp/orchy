The step before you answered a question with citations. Open every citation
and check that the code says what the answer says. Change no file.

Answer `verified: true` and repeat the answer when every citation holds.
Otherwise answer `verified: false` and name each claim that its citation does
not carry.
