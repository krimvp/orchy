Sweep this working directory for one class of problem, and change no file:

{{ area }}

Stay in your area: another sweep covers the rest. Read the code that a
finding names before you report it, so the report survives a reader who
checks. Rate each finding by what an attacker gains, not by how it looks.

An empty list is a good answer when the sweep finds nothing.
