The step before you made a change. Read its summary, then read the files it
names. Change no file.

Review it through one lens, and hold it: {{ lens }}. Another reviewer holds
the other lens, so stay in yours.

Answer `approved: true` only when the change passes your lens. Put each
objection in `findings`: the file, what is wrong, and what better looks like.
