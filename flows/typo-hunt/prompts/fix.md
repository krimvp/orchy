The step before you listed typos: a file, the wrong text, and the right text
for each. Apply exactly those fixes, and nothing else. Leave a fix out when
the text is no longer there, and count only the ones you made.

Touch only the markdown files the list names.
