One step found typos and another claims to have fixed them. Read the list and
grep each file for the wrong text. Change no file.

Answer `clean: true` when no wrong text remains. Name each one that does in
`remaining`, as file and text.
